{
  "name": "inquiry-removal-ai",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered outbound calling system for credit inquiry removal via Retell AI",
  "scripts": {
    "dev": "vercel dev",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "setup-agent": "node scripts/setup-experian-agent.js"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "retell-sdk": "^4.0.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "prettier": "^3.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
